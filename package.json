{
  "name": "parkease",
  "private": true,
  "version": "1.0.0",
  "description": "Root package.json for Netlify build orchestration: installs the dependencies used by the Netlify Function that wraps the Parkease-Backend Express app, and drives the frontend build.",
  "scripts": {
    "build": "npm install && npm --prefix ParkEase-Frontend install && npm --prefix ParkEase-Frontend run build"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.3",
    "serverless-http": "^4.0.0"
  }
}
